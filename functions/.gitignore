# dependencies
node_modules/

# build output
lib/

# env files (never commit secrets — .env.example is intentionally kept; RESEND_API_KEY itself
# never lives in an .env file at all, see src/index.ts / SETUP.md — it's a Secret Manager-backed
# param set via `firebase functions:secrets:set`)
.env
.env.*
!.env.example

# logs
*.log
firebase-debug.log
