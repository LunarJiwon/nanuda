# firebase-tools' Next.js Hosting integration installs `firebase-frameworks`, whose peer range
# (firebase-admin ^11-13) is older than this project's firebase-admin@^14 (used only by
# scripts/seed.ts). Without this, that install step fails with ERESOLVE during `firebase deploy
# --only hosting`.
legacy-peer-deps=true
